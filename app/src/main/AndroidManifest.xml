<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.leeyoungjae.my">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_app"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@drawable/ic_app"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.Activity.PhotoActivity"></activity>
        <activity
            android:name=".View.Activity.MovieListActivity"
            android:label="@string/title_activity_movie_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".View.Activity.WriteCommentActivity" />
        <activity android:name=".View.Activity.AllCommentActivity" />
        <activity android:name=".View.Activity.DetailActivity" />
        <activity android:name=".View.Activity.MainInfoActivity" />
        <activity android:name=".View.Activity.MainActivity" />
        <activity android:name=".View.Activity.Splash_Activity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.Activity.AnotherActivity" />
        <activity android:name=".View.Activity.SignUpActivity" />
    </application>

</manifest>